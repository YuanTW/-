name: 通知 — Momentum TW Stock

on:
  schedule:
    # 台灣時間每個工作日早上 08:00 = UTC 00:00
    - cron: '0 0 * * 1-5'
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安裝套件
        run: |
          pip install --quiet \
            jupyter nbconvert \
            pandas numpy pytz requests \
            yfinance matplotlib plotly

      - name: 執行策略並發送 Telegram 通知
        env:
          TELEGRAM_TOKEN:   ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python notify_runner.py Momentum_TW_Stock.ipynb momentum_tw

      - name: 儲存方向狀態回 repo
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add strategy_states.json
          git diff --staged --quiet || git commit -m "chore: update strategy state [skip ci]"
          git push
